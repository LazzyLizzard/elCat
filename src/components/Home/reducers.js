const initialState = {
    makeId: null,
    year: null,
    transportTypeId: null,
    transportTypeInfo: {},
    makeInfo: {}
};

export default function HomeReducers (state = initialState) {
    return state;
}
